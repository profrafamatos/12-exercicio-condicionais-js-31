/**
  Implemente uma função chamada 'numeroPar' que recebe como parâmetro um número
   inteiro qualquer (numero) e que verifica se um número é par. Se for par retorna 
   o booleano True.

  Sintaxe:
    if (condição) {
        bloco de código que é executado se a condição for true
    }
 */

function numeroPar(numero) {
  /**** Escreva o código abaixo 👇******/

  
  /**** Escreva o código acima ☝️******/
}

module.exports = numeroPar; //Não altere essa linha
